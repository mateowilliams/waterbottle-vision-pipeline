# ----- Model / training -----
# torch/torchvision on CPU. On Linux/Docker they install from the CPU index
# (see Dockerfile); on Windows/Mac the default PyPI wheel is already CPU.
torch==2.9.1
torchvision==0.24.1
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.2.6
pillow==11.1.0

# ----- REST API / serving -----
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21

# ----- Database / migrations -----
psycopg[binary]==3.3.2
alembic==1.18.4
SQLAlchemy==2.0.51

# ----- EDA (notebook) -----
matplotlib==3.10.1
